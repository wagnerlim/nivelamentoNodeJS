function sampleVariableLet(valor) {
  // Quando se declara uma variavel com LET significa que ela pode ser mutavel de acordo com o código.
  let variavelA = valor;
}
sampleVariableLet(process.argv[2]);
